// Relay is a single-surface app: RelayShell (rendered by the layout) owns the
// whole UI and switches panels from the rail. This route exists so that "/"
// resolves inside the authenticated layout.
export default function Page() {
  return null;
}
